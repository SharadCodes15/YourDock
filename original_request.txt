<USER_REQUEST>
Fix the following bugs and issues in the existing Electron dock/menu-bar/drawer/settings app. Do NOT add any new features â€” this pass is fixes and optimization only, no new UI, no new capabilities.

======================
1) FIX: saveConfig is not defined (main.js:2074)
======================
- Search the entire main.js for every call to `saveConfig(...)` â€” this function is being invoked but was never defined (or was defined in a different module and never imported/exported correctly), causing an unhandled promise rejection on every IPC call that touches it.
- Locate wherever config.json is actually being read/written elsewhere in the codebase (there should already be a working read/write pattern used for pinned apps, favorites, or settings) and consolidate into ONE properly defined function, e.g.:
  function saveConfig(configObject) {
    return fs.promises.writeFile(configPath, JSON.stringify(configObject, null, 2), 'utf-8');
  }
- Make sure this function is defined at module scope in main.js (or in a shared config.js module that's require()'d at the top of main.js) BEFORE any IPC handler references it â€” not defined conditionally or inside another function's scope where it won't be visible to the IPC handlers calling it.
- Audit every IPC handler in main.js that touches favorites, badge counts, dock pin/unpin, icon overrides, or settings â€” confirm each one that calls saveConfig has a proper try/catch or .catch() so a future bug like this fails gracefully (log + return an error to renderer) instead of crashing as an unhandled rejection.
- After fixing, verify: toggling a favorite, pinning/unpinning a dock icon, and setting a badge count all persist correctly to config.json without console errors.

======================
2) SMARTER DOCK FILTERING â€” exclude dev tools/background utilities
======================
- The current Start Menu scan filter (keyword blacklist for "uninstall/readme/help/changelog") is too weak â€” it's letting through Node.js, Python, and similar developer runtimes/SD
<truncated 3411 bytes>
not reimplemented per surface.

======================
4) CODE OPTIMIZATION PASS
======================
- Audit main.js for duplicate IPC handlers or duplicate polling intervals accumulated across prior feature additions â€” consolidate any repeated setInterval/setTimeout calls doing similar work (e.g. if battery poll and wifi poll are separate timers, merge into the single shared timer module referenced in earlier work).
- Remove any dead code paths left over from earlier iterations (e.g. old context-menu positioning logic that's now superseded by the fix in section 3 â€” delete it, don't leave it commented out or unused).
- Verify all fs.readFile/writeFile calls related to config.json/apps.json/settings.json use the async promises API consistently (fs.promises or util.promisify) rather than a mix of sync and async calls, which can cause race conditions when multiple IPC calls fire close together (e.g. rapid favorite-toggling).
- Add a single debug flag at the top of main.js (const DEBUG = false) gating any console.log statements added during previous feature builds, so production runs stay quiet unless explicitly enabled.

Output: corrected main.js (saveConfig properly defined + all callers fixed), devToolsBlacklist.json (new filter config file), updated Start Menu scan logic in appscanner.js, unified positionContextMenu function used across dock.html/drawer.html renderers, and a brief inline comment at each fixed location explaining what was wrong (helps track changes across this many iterative prompts). Must not remove or break any existing working feature â€” this is fixes and cleanup only.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-07-15T14:36:22+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from Gemini 3.5 Flash (Low) to Gemini 3.5 Flash (Medium). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>
Created At: 2026-07-15T13:11:28Z
Completed At: 2026-07-15T13:12:18Z

				The command completed successfully.
				Output:
				{"step_index":636,"source":"USER_EXPLICIT","type":"USER_INPUT","status":"DONE","created_at":"2026-07-15T09:06:22Z","content":"<USER_REQUEST>\nFix the following bugs and issues in the existing Electron dock/menu-bar/drawer/settings app. Do NOT add any new features - this pass is fixes and optimization only, no new UI, no new capabilities.\n\n======================\n1) FIX: saveConfig is not defined (main.js:2074)\n======================\n- Search the entire main.js for every call to `saveConfig(...)` - this function is being invoked but was never defined (or was defined in a different module and never imported/exported correctly), causing an unhandled promise rejection on every IPC call that touches it.\n- Locate wherever config.json is actually being read/written elsewhere in the codebase (there should already be a working read/write pattern used for pinned apps, favorites, or settings) and consolidate into ONE properly defined function, e.g.:\n  function saveConfig(configObject) {\n    return fs.promises.writeFile(configPath, JSON.stringify(configObject, null, 2), 'utf-8');\n  }\n- Make sure this function is defined at module scope in main.js (or in a shared config.js module that's require()'d at the top of main.js) BEFORE any IPC handler references it - not defined conditionally or inside another function's scope where it won't be visible to the IPC handlers calling it.\n- Audit every IPC handler in main.js that touches favorites, badge counts, dock pin/unpin, icon overrides, or settings - confirm each one that calls saveConfig has a proper try/catch or .catch() so a future bug like this fails gracefully (log + return an error to renderer) instead of crashing as an unhandled rejection.\n- After fixing, verify: toggling a favorite, pinning/unpinning a dock icon, and setting a badge count all persist correctly to config.json without console errors.\n\n======================\n2) SMARTER DOCK
<truncated 301 bytes>
d per surface.\n\n======================\n4) CODE OPTIMIZATION PASS\n======================\n- Audit main.js for duplicate IPC handlers or duplicate polling intervals accumulated across prior feature additions - consolidate any repeated setInterval/setTimeout calls doing similar work (e.g. if battery poll and wifi poll are separate timers, merge into the single shared timer module referenced in earlier work).\n- Remove any dead code paths left over from earlier iterations (e.g. old context-menu positioning logic that's now superseded by the fix in section 3 - delete it, don't leave it commented out or unused).\n- Verify all fs.readFile/writeFile calls related to config.json/apps.json/settings.json use the async promises API consistently (fs.promises or util.promisify) rather than a mix of sync and async calls, which can cause race conditions when multiple IPC calls fire close together (e.g. rapid favorite-toggling).\n- Add a single debug flag at the top of main.js (const DEBUG = false) gating any console.log statements added during previous feature builds, so production runs stay quiet unless explicitly enabled.\n\nOutput: corrected main.js (saveConfig properly defined + all callers fixed), devToolsBlacklist.json (new filter config file), updated Start Menu scan logic in appscanner.js, unified positionContextMenu function used across dock.html/drawer.html renderers, and a brief inline comment at each fixed location explaining what was wrong (helps track changes across this many iterative prompts). Must not remove or break any existing working feature - this is fixes and cleanup only.\n</USER_REQUEST>\n<ADDITIONAL_METADATA>\nThe current local time is: 2026-07-15T14:36:22+05:30.\n</ADDITIONAL_METADATA>\n<USER_SETTINGS_CHANGE>\nThe user changed setting `Model Selection` from Gemini 3.5 Flash (Low) to Gemini 3.5 Flash (Medium). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.\n</USER_SETTINGS_CHANGE>"}


